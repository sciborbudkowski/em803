{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug target (CMake)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],  // Możesz dodać argumenty tutaj, jeśli są wymagane
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "CMake: build"
    }
  ]
}
